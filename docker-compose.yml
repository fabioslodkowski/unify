services:
  unify:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./app:/var/www/html   # hot-reload durante desenvolvimento
    environment:
      - APACHE_LOG_DIR=/var/log/apache2
    restart: unless-stopped
